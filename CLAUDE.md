Future AI assistants should read the following documentation help-pages:

* `ellmer::Chat`
* `ellmer::tool()`
* `btw::btw()`
* `btw::btw_tools()`
* `shinychat::chat_app()`

Also, read the files in `R/`, `inst/sandbox/tool_results.md`, and `inst/sandbox/shinychat/pkg-r/vignettes/articles/tool-ui.Rmd`.

For read-only context, the source code for the btw, ellmer, shinychat, and side packages is cloned into `inst/sandbox`.

Always run `air format .` after writing R code.
