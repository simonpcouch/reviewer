% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{reviewer_options}
\alias{reviewer_options}
\alias{reviewer.chat}
\alias{reviewer.pending_edits}
\alias{reviewer.memory}
\title{Options used by the reviewer package}
\description{
The reviewer package makes use of the following user-facing options:
\itemize{
\item \code{reviewer.chat} supplies the underlying LLM powering the reviewer.
The \code{chat} argument to \code{\link[=review]{review()}} takes precedence over this option when provided.
The option can be either:
\itemize{
\item An \link[ellmer:Chat]{ellmer::Chat} object (e.g., \code{ellmer::chat_claude()})
\item A model string in \code{"provider/model"} format as described in \code{\link[ellmer:chat-any]{ellmer::chat()}}
(e.g., \code{"anthropic/claude-sonnet-4-5"})
}

Typically set in your \code{.Rprofile}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{options(reviewer.chat = ellmer::chat_claude(model = "claude-sonnet-4-5"))
# or
options(reviewer.chat = "openai/gpt-5")
}\if{html}{\out{</div>}}
\item \code{reviewer.pending_edits} controls the maximum number of pending edits
allowed at once before the model waits for user responses. The
\code{max_pending} argument to \code{\link[=review]{review()}} takes precedence over this option
when provided. The option must be a positive integer. Higher values reduce
wait time but may feel more overwhelming and risk edit conflicts.
Defaults to 2.

Typically set in your \code{.Rprofile}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{options(reviewer.pending_edits = 5)
}\if{html}{\out{</div>}}
\item \code{reviewer.memory} specifies the path to a file where reviewer stores
user preferences learned from accepted/rejected edits. When set, reviewer
uses this file without prompting. When not set, reviewer will ask for
permission to write to \verb{~/.config/reviewer/memory.md} on first launch.

Typically set in your \code{.Rprofile}:

\if{html}{\out{<div class="sourceCode">}}\preformatted{options(reviewer.memory = "~/.config/reviewer/memory.md")
}\if{html}{\out{</div>}}
}
}
